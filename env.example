# Authentication Configuration
# Example environment file - rename to .env and customize for your environment
# 所有变量都必须设置，否则认证将无法工作

# JWT Secret - Use a long, random string in production (必填)
JWT_SECRET=your_jwt_secret_key_here

# 用户凭据 - 这些凭据必须设置 (必填)
# 在生产环境中应使用数据库存储用户信息
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
USER_USERNAME=user
USER_PASSWORD=password123 